{
  "name": "database",
  "version": "1.0.0",
  "description": "Database schema and migrations for MapleStory Web App",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "tsx src/seed.ts",
    "reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "prisma": "^5.7.1"
  },
  "devDependencies": {
    "tsx": "^4.6.2",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5"
  }
}
