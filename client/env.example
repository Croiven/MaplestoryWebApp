# ===========================================
# REACT CLIENT ENVIRONMENT
# ===========================================
# NOTE: Most variables are defined in the root .env file
# This file only contains client-specific overrides if needed

# Override API URL if different from root
# VITE_API_URL=http://localhost:5000/api

# Override app settings if different from root
# VITE_APP_NAME="MapleStory Web App"
# VITE_APP_VERSION="1.0.0"

# Client-specific variables (not in root .env)
# VITE_DEBUG_MODE=true
# VITE_ANALYTICS_ID="your-analytics-id"
