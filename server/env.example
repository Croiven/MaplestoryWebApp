# ===========================================
# SERVER ENVIRONMENT
# ===========================================
# NOTE: Most variables are defined in the root .env file
# This file only contains server-specific overrides if needed

# Override server settings if different from root
# PORT=5000
# NODE_ENV=development

# Server-specific variables (not in root .env)
# SERVER_LOG_LEVEL="debug"
# SERVER_CORS_ORIGINS="http://localhost:3000,https://yourdomain.com"

